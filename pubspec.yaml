name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  iconsax: ^0.0.8
  image_picker: ^1.2.2
  flutter_riverpod: ^2.6.1
  dotted_line: ^3.2.3
  google_fonts: ^6.2.1
  mask_text_input_formatter: ^2.9.0
  firebase_core: ^4.9.0
  cloud_firestore: ^6.4.1
  firebase_auth: ^6.5.1
  firebase_storage: ^13.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    # - assets/Categories/
    # - assets/Curated Items/Women/
    # - assets/Curated Items/Men/
    # - assets/Curated Items/Teens/
    # - assets/Curated Items/Kids/

